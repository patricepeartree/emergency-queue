SCRIPTS:

  "start": "tsc && node build/server.js",
    - DOES NOT RESTART ON TS FILE CHANGES

  "start": "ts-node-dev --inspect=0.0.0.0 --respawn --poll --transpileOnly ./src/app.ts",
    - WHEN RUNNING INSIDE DEV DOCKER CONTAINER
    - TO ALLOW RELOAD ON FILE CHANGES AND NODE DEBUG

  "dev-debug": "DEBUG=pdf-gen* npm run dev",
    - WHEN USING "debug" PACKAGE FOR LOGGING

  "start": "node ./build/node/src/app.js",
  "start-debug": "DEBUG=pdf-gen* node ./build/node/src/app.js"
